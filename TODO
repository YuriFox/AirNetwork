1. Create UPLOAD task

2. func parse(smthParameters?) throws -> SMTH where:
- SMTH is Data, JSON, etc.
- smthParameters is ???
- throw is ANTaskKError

3. Create ANLoadingIndicatorProtocol. ANRequest has property `loading indicator` which will connected to request. When request startted loading indicator started too.
- var isLoading: Bool { set get }

4. Task ID + ANManager.cancelAllTasks + ANManager.cancelTasks(with id: ID)

5. Transform as ObjectMapper

6. README
